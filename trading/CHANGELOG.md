While the below are not timestamped, please ensure any future additions are.

Raised CSV field-size limits in both learned-kernel ingestion (`trading/futures/learned.py`) and shadow analysis loading (`trading/scripts/analyze_shadow_signals.py`) so large diagnostic columns like `shadow_beam_step_counts` no longer break runs with `_csv.Error: field larger than field limit`.
Reran the 0.05 cost-band BTC/SPY shadow check after the parser fix (`logs/shadow/shadow_signal_report_20260313T135734Z_costband005_rerun.md`): `pred_flat` lifted to `0.0865` (BTC) and `0.0737` (SPY), confirming flat survives beam search and now survives basin aggregation in a tri-modal basin.
Ran the 0.10 companion rerun on the same fixed path (`logs/shadow/shadow_signal_report_20260313T143040Z_costband010_rerun.md`): `pred_flat` increased further (`~0.175` on BTC/SPY), curvature dropped (`~0.65`), and action rate collapsed (`BTC 0.0`, `SPY 0.0036`) due to entropy gating, confirming the remaining issue is gate calibration rather than basin structure.
Calibrated lex entropy gating by raising the default `BeamIntentPolicy.entropy_threshold` from `0.92` to `0.96` (kept lex logic simple and explicit); 0.10 rerun with this calibration (`logs/shadow/shadow_signal_report_20260313T145412Z_costband010_ent096.md`) now lands in non-degenerate action rates (`BTC 0.0546`, `SPY 0.1038`) while preserving tri-modal basin mass (`pred_flat ~0.174` on both tapes).
Matched 0.05 rerun under the same `ent096` defaults (`logs/shadow/shadow_signal_report_20260313T151102Z_costband005_ent096.md`) shows action remains near-all-act (`BTC 1.0000`, `SPY 0.9992`), so calibration improved 0.10 but the 0.05 -> 0.10 response is still steep and needs a smoother/adaptive entropy gate.
Implemented smooth entropy attenuation in the shadow gate by multiplying `score_adjusted` with a logistic entropy weight in `BeamIntentPolicy`, and exposed runtime controls in `run_trader.py` (`--shadow-entropy-gate-mode`, `--shadow-entropy-gate-center`, `--shadow-entropy-gate-tau`).
Ran a 3-point smooth-gate sweep with `H0=0.955`, `tau=0.01` at label thresholds `0.05/0.075/0.10` (`logs/shadow/shadow_signal_report_20260313T155304Z_smooth_gate_005.md`, `...155808Z_smooth_gate_075.md`, `...160305Z_smooth_gate_100.md`): basin geometry transitions smoothly but action rate remains near-all-act for 0.05/0.075 and near-all-act for 0.10 under the current score threshold, so score-threshold/adaptive-rate calibration remains required.
Implemented per-asset adaptive quantile score-thresholding in `BeamIntentPolicy` (rolling score history with target action-rate controls) and exposed CLI/runtime flags: `--shadow-score-threshold-mode {fixed,adaptive_quantile}`, `--shadow-target-action-rate`, `--shadow-score-threshold-min-history`, `--shadow-score-threshold-history-size`.
Ran the 3-point adaptive-target sweep (target action rate 10%) at 0.05/0.075/0.10 (`logs/shadow/shadow_signal_report_20260313T162823Z_adapt10_005.md`, `...163325Z_adapt10_075.md`, `...163836Z_adapt10_100.md`): SPY action rates stabilized near target (~0.10–0.113), while BTC remained elevated (~0.184–0.287) on the short tape, indicating residual cold-start/history sensitivity in adaptive thresholding.
Implemented offline/prefit initialization for adaptive-quantile thresholds by seeding `BeamIntentPolicy` score history from prior `shadow_score_adjusted` values in `trading_log*.csv` (per-asset filtered) and added runtime controls `--shadow-score-threshold-prefit` / `--shadow-score-threshold-prefit-max-values` to stabilize short-tape cold starts before rolling updates take over.
Fixed prefit loader robustness for large shadow logs by raising CSV parser field limits inside `_load_prefit_shadow_scores`, eliminating `_csv.Error: field larger than field limit (131072)` on rows containing long structured diagnostics.
Added strict prefit seed scoping via `--shadow-score-threshold-prefit-family` so adaptive initialization can be constrained to a matching run-family substring (on top of symbol matching), preventing cross-regime seed contamination from unrelated historical shadow logs.
Reran adaptive prefit with family-scoped seeding and compared it against both the `adapt10` baseline and the `seed200` unscoped prefit (`logs/shadow/shadow_signal_report_20260315T081325Z_prefit_family_compare.md`): family-scoping worked technically but produced no material action-rate or ACT/HOLD improvement over the `seed200` run, so prefit scope is no longer the highest-value lever.
Extended `scripts/analyze_shadow_signals.py` with failure-locus diagnostics: separate raw-score vs adjusted-score spread, ranking curve (`score bucket -> mean |future return|`), activation curve (`score bucket -> ACT rate`), raw-score vs future-return heatmap, and a heuristic failure-locus summary in the markdown report (`proposal_amplitude`, `ranking`, `activation`, `mixed`).
Added shadow-policy calibration controls: score-mode variants (`ratio`, `scaled_diff`, `logistic`), gating-mode switch (`lex`, `joint`, `score_only`), and shrinkage kernel blending with kernel metadata (`kernel_lambda`, asset/global counts); expanded shadow diagnostics/log fields and updated the shadow analysis script to include score and basin-margin histograms plus action/hold conditional returns and entropy-vs-profitability plots.
Extended the futures shadow diagnostics to include basin curvature and flat-distance metrics, optional curvature-aware gating/score weighting, kernel label-count logging, and a configurable kernel training log directory; completed fixed-threshold sweeps (0.010–0.030 and 0.05–0.25) showing flat labels appear in training but predicted flat mass remains near zero, so the next step is preserving flat candidates in the beam before further gate tuning.
Implemented label-stratified beam retention (quota + overflow) with CLI controls to preserve flat candidates through pruning (`--shadow-beam-label-min-quota`, `--shadow-beam-label-quota-frac`) and added beam survival logging to expose per-depth long/short/flat counts.
Switched basin classification to a label-aware return-band rule with a fee/slippage cost floor (`|predicted_return| < max(flat_return_band, flat_cost_floor)` → flat, plus flat-label persistence) via `--shadow-beam-flat-return-band`/`--shadow-beam-flat-cost-floor`; the 0.05/0.10 retention reruns still showed `pred_flat ≈ 0`, so the next step is a cost-band sweep to validate flat lift-off.
Added `docs/QUANT_PROFESSIONAL_JUDGEMENT_INQUIRY.md` as a quant-facing handoff note summarizing the formal reading order, current futures-shadow math, empirical artifact set, and the concrete judgement calls now needed for tuning and calibration; later tightened it into a shorter review packet for easier external circulation.
Retuned `ActionWeights` in the futures action functional and reran the BTC/SPY multi-mode shadow sweep at `logs/shadow/*_20260313T062106Z.*`; this broke the score-dominated all-hold regime while preserving diagnostics, but overshot into near-all-act behavior, so the next futures step is moderation/calibration rather than further reward amplification.
Implemented the hold-remediation shadow milestone: added structured hold diagnostics, calibrated hold gating, explicit `global` / `per_asset` / `residual` kernel modes, and a reusable `scripts/analyze_shadow_signals.py` comparison script; reruns at `logs/shadow/*_20260313T061237Z.*` showed that attribution is now explicit but all modes remain hold-only and the dominant blocker has shifted to score pressure, so the next futures step is `ActionWeights` retuning.
Reran the BTC/SPY shadow comparison after landing the learned transition kernel and wrote timestamped artifacts at `logs/shadow/*_20260313T045625Z.*`; the learned kernel lowered entropy and restored nonzero flat mass, but `shadow_hold` remained 100% on both tapes, so the next futures milestone is policy-level hold-bias remediation rather than more branch-generation work.
Implemented a learned transition-kernel scaffold in `trading/futures/learned.py` and wired `engine/loop.py --shadow-futures` to fit from historical per-step trader logs with heuristic fallback when logs are unavailable; short sanity replay already showed nonzero flat mass again before the full BTC/SPY post-kernel rerun was completed.
Added timestamped shadow-signal diagnosis `logs/shadow/shadow_signal_diagnosis_20260313T020345Z.md`, locking the next futures milestone as learned transition-kernel replacement rather than threshold tuning after the cleaned `20260312T052900Z` BTC/SPY rerun showed persistently high entropy, all-hold shadow behavior, and zero flat basin mass.
Resolved archived context for the trader via `robust-context-fetch` using thread `Reconsidering Trading Bot` (`69b200ec-a590-83a1-8ee2-032a3712f038` / canonical `18b31603c8832988de325375f56637a44310ebab`) and recorded the decision in `trading/COMPACTIFIED_CONTEXT.md`.
Added `docs/discrete_action_functional.md` describing the repo-local design: coarse state estimator, discrete action functional, beam-search future tree, and intent selector as an optional sidecar controller.
Implemented an initial `trading/futures/` scaffold (`state.py`, `action_functional.py`, `beam.py`) plus a self-contained test so the action-functional design exists as executable code instead of notes only.
Added the observability-first follow-on pieces: shared `signals/coarse_state.py`, `policy/shadow_runner.py`, `futures/basin.py`, `BeamSummary`, and log-field helpers for beam diagnostics.
Wired the observability path into `run_trader.py` / `engine/loop.py` behind `--shadow-futures`, so per-step logs can now carry live/shadow intent fields and beam diagnostics without changing executed actions.
Corrected the shadow-evaluation splice so the shadow policy is evaluated from pre-execution state rather than post-trade state; live/shadow rows now also record `live_fill_intended` and `live_next_pos_intended` for the baseline decision path.
Ran first-pass timestamped BTC/SPY shadow analysis artifacts at `logs/shadow/*_20260312T042358Z.*`; those results remain exploratory and should be rerun after the pre-execution shadow fix for decision-grade comparisons.
Reran the BTC/SPY shadow analysis after the pre-execution splice fix and recorded the new decision-grade baseline at `logs/shadow/*_20260312T052900Z.*` (`shadow_signal_report_20260312T052900Z.md`, `shadow_signal_plots_20260312T052900Z.png`).
Phase-4 density monitor now enforces per-ontology balance ≥25%, bin persistence across ≥6 of the last 8 runs, ≥40 rows per bin, monotonic medians, and effect-size ≥0.0005 (`scripts/phase4_density_monitor.py`); the `strict` profile in `configs/phase4_monitor_profiles.json` captures that bundle and `phase4_monitor_runner.py` applies it for any tape.
Gate status now requires 3 consecutive OPENs before reporting success, and every iteration records a `blocking_reason` string in `logs/phase4/density_monitor.log`/JSON payloads so you know exactly why training is still gated.
Candidate density target: ES/NQ intraday futures (see `docs/es_nq_ingestion_plan.md`) keep the trainable regime clean while reusing the existing proposals/prices contract.
Phase-5 execution simulator (`scripts/phase5_execution_simulator.py`) consumes proposals + prices, applies slip/fee bps, and emits timestamped JSONL entries with `realized_pnl`, `slippage_cost`, and `execution_cost`.
TODO tracker now reflects the tightened gate, ES/NQ tape plan, Phase-5 simulator, and the Phase-6 capital-control sketch so the backlog knows what still needs multi-step completion.
Added ES/NQ ingestion artifact: 5-minute price files, synthetic proposals, `scripts/check_esnq_ingestion.py`, and a monitor run that now reports ES/NQ gates as `OPEN` once the strict profile runs for ≥6 iterations.
Added a clarification in `docs/phase4_phase5_strategy.md` about synthetic/amplitude-injected OPENs being mechanical tests and noted how to flag those runs so Phase-4.1 only fires off real data.
`scripts/phase4_density_monitor.py` now accepts `--test-vector` so amplitude-injected runs mark `test_vector` in `logs/phase4/density_monitor.log`, and `docs/phase4_density_monitor.md` explains how to interpret that tag plus how to refresh BTC/ES/NQ data using `python data_downloader.py`.
Streaming ingestion now supports live DuckDB writes per closed chunk via `ingest_dataframe`, with optional archive-only mode; chunk duration and run duration accept seconds for faster tests (`data_downloader.py`, `tools/ingest_archives_to_duckdb.py`).
Planned Phase 06 live stream daemon: create `.planning/phases/06-live-stream-daemon/01-daemon-contract-PLAN.md` to define the always-on ingest contract, daemon CLI, and test harness.
Implemented TCP NDJSON stream daemon contract and tooling: `docs/stream_daemon.md`, `scripts/stream_daemon.py`, `scripts/stream_daemon_test.py`, plus a Phase 06 summary.
Planned Phase 06 follow-on work in `.planning/phases/06-live-stream-daemon/02-decision-emit-PLAN.md` for decisions, tail ingest, and metrics.
Extended the stream daemon to emit triadic decisions with Phase 6 gating, persist actions/state tables, replay NDJSON via tail mode, and expose a lightweight metrics endpoint; updated docs and test harness.
Added decision sinks for stream actions (NDJSON file, TCP fan-out, latest-action view) and a read-only probe option in the stream daemon test harness.
Added `scripts/stream_daemon_live.py` to run the daemon and stream live Binance trades in one command.
`scripts/stream_daemon_live.py` now supports `--symbols` lists or `--all-symbols` with `--max-symbols` caps.
Added `scripts/plot_stream_decisions.py` to plot signed target exposure from decision NDJSON with optional state/urgency/posture overlays.
`scripts/plot_stream_decisions.py` now supports live follow mode and WebM timelapse output.
`scripts/stream_daemon_live.py` can now spawn the live plotter or WebM timelapse so a single command runs daemon + feed + plot.
Drafted Phase 07 live density feeder plan at `.planning/phases/07-live-density/01-live-density-PLAN.md`.
Drafted Phase 06.03 plan for fee-aware live dashboard + decision cost gate at `.planning/phases/06-live-stream-daemon/03-fee-aware-dashboard-PLAN.md`.
Implemented budgeted decision cost gate flags in `scripts/stream_daemon.py` (cost-aware clamp + metrics) and wired them through `scripts/stream_daemon_live.py`; updated the live dashboard to step-plot targets, add delta exposure markers, and compute notional fees in `scripts/plot_stream_dashboard_pg.py`.
Updated M9/all-red spine write-up in `TRADER_CONTEXT.md` to reflect gate-first hazard semantics, non-extractive supervision, and special-code severity ordering.
Added post-alignment refusal-first notes in `TRADER_CONTEXT.md` covering eigen-orbit census framing, fee-as-norm completion, and Phase-07 to Phase-04 gating implications.
Added false eigen-event and friction boundary term formalization in `TRADER_CONTEXT.md`, plus a Phase-07 asymmetry density gate tied to Phase-04 unblocking.
Added a DASHI formalism -> current trader projection section in `TRADER_CONTEXT.md` with explicit gap analysis and operational consequences.
Locked in M5/UNKNOWN control-law language and profit eigen-event criteria in `TRADER_CONTEXT.md`.
Pre-implementation exploration: repo scan and skill doc review; context search for M5/UNKNOWN/eigen-event/Phase-07 notes; reviewed `strategy/triadic_strategy.py`, `signals/triadic.py`, `scripts/run_proposals.py`, and `run_trader.py`; inspected risk handling in `policy/thesis.py`; checked Phase-4
    monitor + gate status + asymmetry sensor for Phase-07 link
        ; searched for legitimacy density usage and net vs gross profit handling; confirmed no `CHANGELOG.md`.
Key paths: intent derivation lives in `strategy/triadic_strategy.py` (UNKNOWN/FLAT handling), triadic state generation in `signals/triadic.py`, proposal vetoes + UNKNOWN abstain in `scripts/run_proposals.py`, trading loop entry in `run_trader.py`/`engine/loop.py`, thesis risk overrides in `policy
    thesis.py`, Phase-4 gate in `scripts/phase4_density_monit
or.py` with summary in `scripts/phase4_gate_status.py`, and Phase-07 signal source in `signals/asymmetry_sensor.py`.
Phase-4 density monitor now requires Phase-07 readiness via a JSONL status log (`--phase7-log`) with a persistence window; gate summaries surface Phase-07 readiness and reasons.
Added Phase-07 diagnostics tooling: `scripts/phase7_asymmetry_diagnostics.py` reports edge/cost density, activity rate, and robustness from decision logs; `docs/phase7_status_emitter.md` documents the workflow and TODOs track the required runs.
Documented live decision interpretation for `posture_observe` and Phase-6 gating in `docs/stream_daemon.md` and `docs/phase6_capital_control.md`, and added a TODO to capture gate status when observe persists.
Added a narrative capture of the posture_observe doc/TODO update process in `docs/posture_observe_notes.md`.
Added `scripts/phase07_eigen_boundary_check.py` stub to compute rho_A across horizons with cost perturbation robustness checks.
Formalized Phase-8 entry criteria and boundary clamp semantics in `COMPACTIFIED_CONTEXT.md`.
Added kernel-Vulkan implementation contract appendix in `docs/appendix_kernel_vulkan.md`.
Added M8 precheck projection to the tower log (non-binding readiness audit) and plotted it in the PyQtGraph internals view (`trading_io/tower_projection.py`, `training_dashboard_pg.py`).
Added Phase-7.3 horizon sweep emitter for tower logs (`scripts/phase73_horizon_sweep.py`) plus near-miss ranking and M8->M9 invariant checking tools (`trading_io/near_miss_rank.py`, `trading_io/m8_m9_invariants.py`).
Dashboard internals now skip tower-only plots when the tower log is missing, keeping the view empty with a one-time warning instead of crashing (`training_dashboard_pg.py`).
Aligned Phase-07 docs with the action-channel boundary certificate (edge/cost density, no return-based medians), including updated Phase-07 emitter and Phase-4 gating documentation.
Phase-07 status emitter and diagnostics now consume decision/action NDJSON logs and report edge/cost density with robustness gates (`scripts/phase7_status_emitter.py`, `scripts/phase7_asymmetry_diagnostics.py`).
Phase-7.3 horizon sweep output now includes `rho_A_null`, `delta_rho_A`, `noise_ratio`, and `class_label` fields for noise-floor annotation (`scripts/phase73_horizon_sweep.py`).
Implemented optional shadow-only score calibration via per-asset z-score standardization with pooled shrinkage, including CLI/runtime controls for calibration mode, threshold source, minimum history, shrinkage scale, and standard-deviation floor.
Extended beam decision diagnostics/logging so shadow rows now expose explicit raw, standardized, and adjusted score stages plus effective calibration mean/std/weight.
Updated the shadow signal analyzer to read the explicit raw/standardized score fields and report standardized-score spread alongside the existing failure-locus diagnostics.
Recorded the current branch decision in docs/context: SPY failure-locus reports show usable raw-score spread and positive ranking uplift, so score standardization is now the next A/B branch and uncertainty-block ablation remains deferred.
